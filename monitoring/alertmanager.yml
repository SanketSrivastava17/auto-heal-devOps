route:
  group_by: ['alertname']
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'autoheal-webhook'

receivers:
- name: 'autoheal-webhook'
  webhook_configs:
  - url: 'http://autoheal-engine:8000/webhook'
    send_resolved: true
